import React from "react";
import styled, { keyframes, css } from "styled-components";

const Add = (props)=>{
    console.log(props);

    return(
        <div>
            
            <input type="text"></input>
            <input type="text"></input>

        </div>
    );


}

export default Add;