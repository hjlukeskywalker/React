import React from "react";
import styled, { keyframes, css } from "styled-components";

const Add = (props)=>{
    console.log(props);

    return(
        <div>일정추가 페이지</div>
    );


}

export default Add;